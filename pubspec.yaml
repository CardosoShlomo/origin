name: origin
description: "Interactive hero transitions framework for Flutter with composable gesture physics."
version: 2.1.1
repository: https://github.com/CardosoShlomo/origin

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
