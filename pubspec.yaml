name: origin
description: "Interactive hero animations."
version: 0.0.3
repository: https://github.com/CardosoShlomo/origin

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
